import { apiClient } from "../api-client"; import { authService as mock } from "../services"; import type { User } from "../services"; import type { ApiEnvelope } from "../api-types";
export const authApi = { signIn: async (email: string, password: string): Promise<ApiEnvelope<User>> => apiClient.isMockMode() ? mock.signIn(email, password) : apiClient.post<User>("/auth/sign-in", { email, password }), sendResetLink: async (email: string) => apiClient.isMockMode() ? mock.sendResetLink(email) : apiClient.post<null>("/auth/reset", { email }), signOut: async () => apiClient.isMockMode() ? mock.signOut() : apiClient.post<null>("/auth/sign-out") }; export default authApi;
